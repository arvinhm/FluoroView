# FluoroView v3 backend — optional companion API for model-backed analysis.
fastapi>=0.111
uvicorn[standard]>=0.30
numpy>=1.24,<2
pillow>=10.0
scikit-image>=0.22
scikit-learn>=1.3
python-multipart>=0.0.9

# --- Optional heavy backends (uncomment to enable model-backed segmentation) ---
# cellpose>=3.0          # Cellpose / Cellpose-SAM
# stardist>=0.9          # StarDist H&E nuclei (2D_versatile_he)
# umap-learn>=0.5        # true UMAP embedding
# torch>=2.2             # required by several segmentation backends
